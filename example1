
#!/bin/bash

echo "Enter the filename:"
read filename

if [ -r "$filename" ] && [ -w "$filename" ]; then
    echo "You have read and write permissions for '$filename'."
else
    echo "You do not have read and write permissions for '$filename'."
fi





